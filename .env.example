# MLflow Configuration
MLFLOW_TRACKING_URI=file://./mlruns
MLFLOW_EXPERIMENT_NAME=obesity_classification
MLFLOW_EXPERIMENT_ID=1

# AWS Configuration for DVC
# IMPORTANT: Replace with your actual AWS credentials
AWS_ACCESS_KEY_ID=your_aws_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key_here
AWS_DEFAULT_REGION=us-east-1
AWS_S3_BUCKET=itesm-mna
AWS_PROFILE=equipo52

# DVC Configuration
DVC_REMOTE_NAME=remote-equipo52
DVC_REMOTE_URL=s3://itesm-mna/202502-equipo52/dvc-storage

# Model Configuration
MODEL_RANDOM_STATE=42
MODEL_TEST_SIZE=0.2

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace placeholder values with actual credentials
# 3. NEVER commit .env to Git (already in .gitignore)
